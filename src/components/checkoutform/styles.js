import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  root: {},
  formProvider: {
    textAlign: "center",
    paddingBottom: "30px",
  },
  selectInput: {
    color: "red",
  },
}));
